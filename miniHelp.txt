docker build -t front .

winda
docker run -p 3000:3000 ^
  -e BACKEND_UPLOAD_URL=http://host.docker.internal:8080/api/upload ^
  front

linuxa
docker run -p 3000:3000 \
  -e BACKEND_UPLOAD_URL=http://host.docker.internal:8080/api/upload \
  front

